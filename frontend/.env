# Automatic API URL detection based on hostname
# Frontend will automatically use http://[same-hostname]:8000
# Works on localhost, 192.168.x.x, or any other IP/hostname

# This allows the React dev server to be accessed from other devices
HOST=0.0.0.0
